const TitleAdminPages = ({ title }: { title: string }) => {
  return (
    <h3 className="text-md font-medium uppercase tracking-[0.24em] text-slate-500">
      {title}
    </h3>
  );
};

export default TitleAdminPages;
